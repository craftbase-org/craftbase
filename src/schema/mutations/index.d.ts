import type { DocumentNode } from 'graphql'

export declare const INSERT_USER_ONE: DocumentNode

// Other mutation exports are typed loosely until Stage 3 converts this file
// against the codegen output. Adding them as ambient DocumentNodes here would
// drift from the generated types; leaving them inferred from JS for now.
